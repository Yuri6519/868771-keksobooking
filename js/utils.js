// модуль содержит общие константы и методы

'use strict';

(function () {
  var KEY_RETURN = 13;
  var KEY_ESCAPE = 27;

  var VAL_ANY = 'any';

  window.utils = {
    KEY_RETURN: KEY_RETURN,
    KEY_ESCAPE: KEY_ESCAPE,
    VAL_ANY: VAL_ANY,
  };

})();
